#!/usr/bin/env python3
"""sum_list which takes a list input_list of floats and returns their sum"""

from typing import List


def sum_list(input_list: List[float]) -> float:
    """Sums up the elements in the input_list."""
    return sum(input_list)
